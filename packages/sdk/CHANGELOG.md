# @ooopsstudio/sdk

## 0.1.0

- Added schema-agnostic immutable event definitions and isolated Zod behind `events/zod`.
- Hardened Events, AsyncAPI and Jobs helpers with bounded validation and deterministic errors.
- Documented every public integration subpath with lifecycle-safe examples.
- Framework-agnostic helpers for events, cache, jobs, server performance, browser performance, database instrumentation, and Faro.
