/**
 * @file Re-exports for error handling service convenience.
 * All types are now defined in @ooopsstudio/core.
 */

import type {ErrorSeverity, ErrorCategory, EnrichedError} from '@ooopsstudio/core/contracts/errors'

export type {ErrorSeverity, ErrorCategory, EnrichedError}
