name: CI

on:
  pull_request:
  push:
    branches:
      - '**'

jobs:
  call-central-ci:
    uses: "Ooops-Studio/.github/.github/workflows/ci.yml@main"
    secrets:
      NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
      RELEASE_TOKEN: ${{ secrets.RELEASE_TOKEN }}